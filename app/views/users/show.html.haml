= provide(:title, @user.name)
// = @user.name
// ,
// = @user.email
// The line below does the same thing as the lines above.
// #{@user.name}, #{@user.email}

%div.row
  %aside.span4
    %section
      %h1
        = gravatar_for @user
        = @user.name